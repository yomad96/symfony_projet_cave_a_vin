<!DOCTYPE html>

<title>Cave</title>

{% block body %}
    <h1>Cave</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cave.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cave.Name }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ cave.Adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cave_edit', {'id': cave.id}) }}">edit</a>

    {{ include('cave/_delete_form.html.twig') }}
{% endblock %}
